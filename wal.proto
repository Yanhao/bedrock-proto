syntax = "proto3";
package wal_pb;

message Entry {
    uint64 chunk_id = 1;
    uint32 offset = 2;
    uint32 length = 3;
    bytes data = 4;
}

message Header {
    uint32 magic = 1;
    uint64 first_index = 2;
    uint64 last_index = 3;
}

